# 第 0 步：目标技能完整能力与工作流分析

> 这是改造的前置门槛。分析不完整，禁止进入判断关卡挖掘。

## 目的

改造前先彻底搞清"目标技能到底是什么、怎么工作的、替用户做了哪些判断"。分析得越准，判断关卡挖得越准，训练质量越高。

## 材料清单（能拿到多少拿多少）

- SKILL.md 全文：frontmatter（name/description，即触发条件与能力范围）+ 正文（工作流、规则）
- references/ 下所有文档：领域知识、模板、细则
- scripts/ 的脚本与说明：确定性处理逻辑
- assets/：输出模板
- 历史使用记录 / 该技能在其他场景的产出样例（如有）
- 目标技能配套的工具/MCP/接口说明（如有）

**材料缺失处理**：只有技能名或只有简介时，先向用户索取完整材料，或按下面"能力访谈"向用户提问补齐，禁止凭名字猜测能力。

## 五问分析法

对目标技能逐项回答，产出三样分析物：

### 1. 它是什么（能力边界）
从 frontmatter 的 description 提取：
- 触发条件：用户什么请求会激活它
- 能力范围：它能处理的任务类型
- 明确不做什么：description 或正文里写的边界（如"不处理 PDF"）

### 2. 它处理什么输入
- 用户给它什么：文本、文件、数据、图片、URL？
- 输入格式与前置条件？

### 3. 它产出什么输出
- 产物类型：文档、代码、图、表格、方案？
- 输出格式与质量标准（正文里通常有"输出格式"节）？

### 4. 它的工作流程（执行步骤）
逐条梳理 SKILL.md 正文的流程，记录：
- 步骤序列（从接需求到交付）
- 每步调用什么：规则表、数据文件、脚本、外部工具、检查清单
- 分支与条件（"如果…则…"）

### 5. 它替用户做了哪些判断（隐含判断点）
在每个执行步骤里找"AI 替用户做决定"的地方。**判断点识别特征**（出现以下信号就是潜在判断点）：

| 信号 | 例子 |
|---|---|
| 归类/定位 | "把请求归类为 X 类型" |
| 选型/取舍 | "在 A/B/C 中选一个"、"优先 X" |
| 风格/语气/结构 | "用正式语气"、"采用三段式" |
| 参数设定 | "默认 3 条结果"、"阈值 0.5" |
| 质量判定 | "符合规范吗"、"checklist 打勾" |
| 反模式规避 | "不要用 X"、"避免 Y" |
| 风险权衡 | "如果…则保守处理" |
| 需要用户上下文 | "取决于你的目标/品牌/用户" —— 技能覆盖不到、必须用户提供 |

## 分析产物模板

```markdown
## 目标技能分析
### 能力边界
- 触发：…
- 能：…
- 不做：…
### 输入/输出
- 输入：…
- 输出（格式+质量标准）：…
### 工作流程
1. 接需求 → …
2. 归类 → …
3. … → 交付
### 隐含判断点清单
1. 判断点：…（对应流程第几步）
2. 判断点：…
```

## 确认关卡

分析完成后：**向用户呈现"能力边界 + 工作流程 + 隐含判断点清单"，确认无误后再进入第 1 步挖掘判断关卡地图。** 用户纠正任何一点都要先更新分析，再继续。
