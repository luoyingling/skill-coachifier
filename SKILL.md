---
name: skill-coachifier
description: "把任意既有 SKILL 改造成带'判断力训练模式'的三模式版本。当用户要求'给某个技能加训练模式/教练模式/light-coach/full-coach/边做边练/训练我的判断力'，或希望一个既有技能同时承担'做事 + 训练'双重职责时使用。工作方式：先完整分析目标技能的能力边界与工作流程（前置门槛），挖掘其输出中隐含的专业判断关卡，再附加 light-coach（边做边练：输出时暴露每个判断的 why，并在需要用户输入处引导提问）与 full-coach（专项训练：先猜后答五步闭环）两种协议，产出'快速 / light-coach / full-coach'三模式技能协议。不修改目标技能本身的代码与数据，只生成附加执行协议。"
---

# Skill Coachifier —— 给任意 Skill 增加训练模式

## 用途

把一个既有技能（目标技能）改造成**三模式**版本，让它从"只给答案的引擎"变成"边做事边训练判断力的教练"：

| 模式 | 用途 | 触发 |
|---|---|---|
| 快速模式 | 原始能力，完全保留 | 用户说"快速"或直接要结果 |
| light-coach | 边做边练：输出每个关键判断时暴露 why；需用户定夺处提问 + 给判断依据引导 | 默认 |
| full-coach | 专项训练：出题→作答→揭示→复盘→评分 五步闭环 | 用户说"训练我 / full" |

改造只生成"执行协议"（附加层），不要求改动目标技能的代码、数据或文件。

## 工作流（四步，顺序不可打乱）

### 第 0 步：完整分析目标技能（前置门槛，不可跳过）

改造三步之前，**必须先完整分析目标技能的完整能力与工作流程**。禁止只看名称或摘要就开始改造。

1. 完整读取目标技能的全部材料：SKILL.md 全文（frontmatter + 正文）、references/、scripts/ 说明、assets/、可用的历史使用记录。
2. 按 `references/analysis-method.md` 产出三样分析物：
   - **能力边界**：处理什么输入、产出什么、不做什么
   - **工作流程**：从输入到输出的执行步骤，每步调用什么规则/数据/工具
   - **隐含判断点清单**：产出成果时 AI 替用户做出的每一个专业决策
3. 向用户呈现分析结果并确认无误后再进入第 1 步；材料缺失（如只有技能名）时，先索取材料或做能力访谈，**禁止猜测**。

### 第 1 步：挖掘判断关卡地图

从第 0 步的流程中，把每个"替用户做决定"的环节提取为判断关卡（通常 5–10 个），逐个标注：能否自动完成、依据来源、用户需复核什么、哪些必须靠用户上下文。产出**判断关卡地图**表格。模板与示例见 `references/judgment-map.md`。

### 第 2 步：附加 light-coach 协议

按 `references/light-coach-protocol.md` 生成"边做边练"协议，核心两条：
- **暴露 why**：输出每个关键判断时显式标注依据（来自目标技能哪条规则/数据/推理链）；
- **引导式提问**：需用户定夺处明确提问，并告诉用户"**你可以基于 ____ 来判断**"（尤其技能覆盖不到的、需结合用户业务/品牌/目标的输入）。

提问克制：每个任务最多 2–3 个必答问题，其余可跳过，不打断工作流。

### 第 3 步：附加 full-coach 协议

按 `references/full-coach-protocol.md` 生成"专项训练"协议：五步闭环（出题→用户作答→揭示参考答案→差距复盘→评分记录），评分维度按该技能的能力定义，并建议训练记录机制（如 `训练记录.md`）。

## 输出格式（交付物）

生成一份**改造后的完整技能协议**，包含：
1. 三模式开关规则（默认 light-coach；"快速"→原始模式；"训练/full"→专项模式）
2. 判断关卡地图（第 1 步成果）
3. light-coach 输出模板（判断点标注格式 + 引导提问格式）
4. full-coach 五步流程模板 + 评分卡
5. 可选的训练记录机制建议

协议应可直接复制回目标技能（如追加到其 SKILL.md 的"教练模式"章节）或作为独立提示词使用。

## 关键纪律

- 判断点必须来自目标技能实际内容，**禁止编造该技能不存在的功能**。
- 保留目标技能原有全部能力与输出，只做附加层，不替换原逻辑。
- 分析不全不改造；用户未确认第 0 步结果不进入第 1 步。
- 若目标技能材料无法获取，明确说明缺失项，不降级为猜测式改造。

## 资源导航

- `references/analysis-method.md` —— 第 0 步：技能完整能力与工作流分析方法（必读，含判断点识别特征与五问法）
- `references/judgment-map.md` —— 第 1 步：判断关卡地图模板 + 完整示例
- `references/light-coach-protocol.md` —— 第 2 步：light-coach 协议模板 + 输出示例
- `references/full-coach-protocol.md` —— 第 3 步：full-coach 协议模板 + 评分卡
